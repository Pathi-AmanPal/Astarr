fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6
duckdb>=0.10
pandas>=2.2
scikit-learn>=1.4
shap>=0.44
PyYAML>=6.0
# Required by FastAPI to read multipart/form-data, i.e. the CSV upload endpoint.
# Pure Python, no network use at runtime -- it is vendored into the offline image by
# the same pip install as everything else.
python-multipart>=0.0.9
